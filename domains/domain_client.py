import requests, pygame

running = True
while running:
    for event in pygame.event.get():
        # Check for closing the game window
        if event.type == pygame.QUIT:
            running = False
    # Get the game state
    # Render the game state
    # update the game state



#response = requests.get("http://localhost:8022/ping")
#print(response.json())
